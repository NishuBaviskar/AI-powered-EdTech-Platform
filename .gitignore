# Dependency directories
# These are the largest folders and can be reinstalled with 'npm install'
node_modules/
client/node_modules/
server/node_modules/

# SENSITIVE INFORMATION
# This is the most important rule. It prevents your API keys and passwords from being uploaded.
.env
.env.local
.env.development.local
.env.production.local

# Build folder from the React client
# This is the compiled code that Render generates, so we don't need to store it.
client/dist/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.vscode/
.idea/

# Operating System files
.DS_Store
Thumbs.db